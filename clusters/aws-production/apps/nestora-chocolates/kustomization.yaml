apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - pdb.yaml

commonLabels:
  app: nestora-chocolates
  environment: production
  managed-by: flux

images:
- name: ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/nestora-chocolates
  newTag: latest